import './index.js';
import { colorGuide } from './colors.js';

var getTagColor = function getTagColor(type, mode) {
  var colorConfig = colorGuide[mode == 'light' ? 'lightComponents' : 'darkComponents'].tags;

  switch (type) {
    case 'success':
      return colorConfig.success;

    case 'error':
      return colorConfig.error;

    case 'warning':
      return colorConfig.warning;

    default:
      return colorConfig.error;
  }
};

export { getTagColor };
//# sourceMappingURL=tags.js.map
