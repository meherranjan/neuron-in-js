// input -> process -> output
// x -> f(x) -> y

function perceptron(x1, x2) {
  let weightedInputOne = x1 * w1;
  let weightedInputTwo = x2 * w2;
}

class Perceptron {
  constructor(n) {
    let weights = [];
    for (let i = 0; i < weights.length; i++) {
      weights[i] = Math.random();
    }
  }
}


predictor([10, 22.04]);
