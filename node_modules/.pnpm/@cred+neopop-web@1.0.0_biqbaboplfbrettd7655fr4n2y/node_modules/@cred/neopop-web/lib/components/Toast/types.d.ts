import { FontNameSpaceInterface } from '@types';
export declare type ToastItemProps = {
    fullWidth: boolean;
    background: string;
    autoCloseTime: number | undefined;
};
export declare type ToastIconProps = {
    icon: string | undefined;
};
export declare type OptionProps = Omit<ToastProps, 'id' | 'removeToast' | 'colorConfig'>;
export declare type RefType = {
    addToast: (options?: OptionProps) => void;
};
declare type ToastColorConfig = {
    background: string;
    color: string;
};
export declare type ToastTypes = 'success' | 'error' | 'warning';
export interface ToastProps {
    id?: string;
    content: string;
    description?: string;
    textStyle?: {
        heading: FontNameSpaceInterface;
        description: FontNameSpaceInterface;
    };
    type?: ToastTypes;
    colorConfig: ToastColorConfig;
    fullWidth?: boolean;
    dismissOnClick?: boolean;
    autoCloseTime?: number;
    icon?: string;
    removeToast?: (id: string) => void;
}
export {};
//# sourceMappingURL=types.d.ts.map